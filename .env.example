# GitHub Token for mise tools installation (to avoid rate limits)
# <https://github.com/settings/personal-access-tokens/new?name=node_reqwest-env>
# GITHUB_TOKEN= # github_token_with_read_only_access_to_public_repositories

# Docker build user settings (for matching host file permissions)
# USER_UID= # $ id -u
# USER_GID= # $ id -g

# Mitmproxy web UI password (access at http://127.0.0.1:8081/?token=YOUR_PASSWORD)
# MITMPROXY_WEB_PASSWORD= # YOUR_PASSWORD
