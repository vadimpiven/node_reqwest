{
  "private": true,
  "name": "meta",
  "devDependencies": {
    "shx": "catalog:"
  },
  "scripts": {
    "test:nextest": "cargo llvm-cov nextest --lcov --output-path lcov.info",
    "posttest:nextest": "pnpm exec shx cp ../../target/nextest/default/junit.xml report-rust.junit.xml",
    "test:doctest": "cargo test --doc",
    "test:rustdoc": "cargo doc --no-deps --document-private-items",
    "test": "pnpm run test:nextest -r --locked && pnpm run test:doctest -r --locked && pnpm run test:rustdoc -r --locked",
    "dev-test": "pnpm run test:nextest && pnpm run test:doctest && pnpm run test:rustdoc"
  },
  "version": "1.2.3"
}
