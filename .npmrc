# Limit concurrency to prevent conflicts with shared Cargo target directory
workspace-concurrency=1

# Native pnpm store location (Docker overrides via PNPM_STORE_PATH env var)
store-dir=.cache/native/pnpm-store
