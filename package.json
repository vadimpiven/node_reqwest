{
  "private": true,
  "name": "workspace",
  "type": "module",
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "pnpm": "^10.27.0",
    "node": "^22.21.1",
    "runtime": {
      "name": "node",
      "version": "^22.21.1",
      "onFail": "download"
    }
  },
  "devDependencies": {
    "@biomejs/cli-darwin-arm64": "catalog:",
    "@biomejs/cli-darwin-x64": "catalog:",
    "@biomejs/cli-linux-arm64-musl": "catalog:",
    "@biomejs/cli-linux-x64-musl": "catalog:",
    "@biomejs/cli-win32-arm64": "catalog:",
    "@biomejs/cli-win32-x64": "catalog:",
    "@playwright/test": "catalog:",
    "@taplo/cli": "catalog:",
    "@types/node": "catalog:",
    "husky": "catalog:",
    "markdownlint-cli2": "catalog:",
    "vitest": "catalog:"
  },
  "scripts": {
    "postinstall": "node scripts/setup-path.ts",
    "playwright": "pnpm exec playwright install-deps",
    "rustup": "rustup update && rustup toolchain install",
    "check:biome": "node scripts/biome.ts check . --log-level warn",
    "check:taplo": "pnpm exec taplo check",
    "check:markdownlint": "pnpm exec markdownlint-cli2",
    "check:clippy": "cargo clippy -r --locked -- -D warnings",
    "check:rustfmt": "cargo fmt --check",
    "check:zizmor": "uv run --no-sync zizmor -q .github",
    "check:semgrep": "uv run --no-sync --env-file .python.env semgrep scan --error -q",
    "check": "pnpm run check:biome && pnpm run check:taplo && pnpm run check:markdownlint && pnpm run check:clippy && pnpm run check:rustfmt && pnpm run check:zizmor && pnpm run check:semgrep",
    "fix:biome": "pnpm run check:biome --fix",
    "fix:taplo": "pnpm exec taplo format",
    "fix:markdownlint": "pnpm exec markdownlint-cli2 --fix",
    "fix:clippy": "cargo clippy --fix --allow-dirty",
    "fix:rustfmt": "cargo fmt",
    "fix:zizmor": "pnpm run check:zizmor",
    "fix:semgrep": "pnpm run check:semgrep --autofix",
    "fix": "pnpm run fix:biome && pnpm run fix:taplo && pnpm run fix:markdownlint && pnpm run fix:clippy && pnpm run fix:rustfmt && pnpm run fix:zizmor && pnpm run fix:semgrep",
    "prepare": "husky",
    "clean:pnpm": "pnpm store prune",
    "clean:cargo": "cargo bin cargo-cache --autoclean && cargo clean",
    "clean:rustup": "node scripts/clean-rustup.ts",
    "clean:docker": "docker compose down --volumes --remove-orphans && docker system prune -af",
    "clean": "pnpm run clean:pnpm && pnpm run clean:cargo && pnpm run clean:rustup && pnpm run clean:docker",
    "check-updates:pnpm": "(pnpm outdated --recursive --sort-by name || exit 0)",
    "check-updates:cargo": "cargo update --breaking -Z unstable-options --dry-run",
    "check-updates:uv": "uv lock --upgrade --dry-run",
    "check-updates": "pnpm run check-updates:pnpm && pnpm run check-updates:cargo && pnpm run check-updates:uv",
    "update:pnpm": "pnpm update --recursive --workspace && pnpm audit --fix && pnpm install",
    "update:cargo": "cargo bin cargo-cache --autoclean && cargo clean && cargo update",
    "update:uv": "uv lock --upgrade",
    "update": "pnpm run update:pnpm && pnpm run update:cargo && pnpm run update:uv",
    "ci-build": "pnpm run --recursive ci-build",
    "preci-test": "pnpm run check",
    "ci-test": "pnpm run --recursive ci-test",
    "build": "pnpm run --recursive build",
    "pretest": "pnpm run fix",
    "test": "pnpm run --recursive test"
  },
  "dependencies": {}
}
