{
  "private": true,
  "name": "workspace",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@taplo/cli": "catalog:",
    "@types/node": "catalog:",
    "markdownlint-cli2": "catalog:",
    "vitest": "catalog:"
  },
  "scripts": {
    "docker": "docker compose up --build -d --wait && docker compose exec dev bash",
    "postdocker": "docker compose down --remove-orphans"
  }
}
