{
  "private": true,
  "name": "workspace",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "catalog:",
    "@taplo/cli": "catalog:",
    "@types/node": "catalog:",
    "husky": "catalog:",
    "markdownlint-cli2": "catalog:",
    "vitest": "catalog:"
  },
  "scripts": {
    "prepare": "husky",
    "predocker": "docker compose up --build -d --wait",
    "docker": "docker compose exec dev bash",
    "postdocker": "docker compose down --remove-orphans"
  }
}
